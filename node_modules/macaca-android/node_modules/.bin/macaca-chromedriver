#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../macaca-chromedriver/bin/macaca-chromedriver" "$@"
  ret=$?
else 
  node  "$basedir/../macaca-chromedriver/bin/macaca-chromedriver" "$@"
  ret=$?
fi
exit $ret
